# build artifacts (anchored to repo root so it never matches cmd/truemtls/)
/build/
/truemtls
*.test

# local certs / keys / trust material — never commit
*.pem
*.key
*.crt
*.p12
/cfg/

# editor/OS
.DS_Store
*.swp
